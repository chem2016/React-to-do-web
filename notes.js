/*
1. Redux for state manager
2. CLI tool for creating reaction applications
    use webpack(module bundler)
3. Anatomy of a component

Class Post extends from React.Component{
    super()
    state = {
        title: 'Post One',
        body: 'This is my post'
    }
    render(){     // this return JSX-> like html
        return (
            <div>
                <h3>{ this.state.title }</h3>   // can be any js script
                <p>{ this.state.body }</p>
            </div>
        )
    }
}
   
A page has multiple component




*/ 

